import * as Blockly from "blockly";
import "@blockly/field-slider";
Blockly.defineBlocksWithJsonArray([
  {
    type: "field_slider",
    message0: "%1",
    args0: [
      {
        type: "field_slider",
        name: "FIELDNAME",
      },
    ],
  },
]);
